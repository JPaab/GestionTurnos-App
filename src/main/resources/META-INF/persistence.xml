<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             version="3.0">
    <!-- aqui defino la unidad de persistencia que uso en ConfigJPA -->
    <persistence-unit name="TurnosPU" transaction-type="RESOURCE_LOCAL">
        <properties>
            <!-- aqui configuro la conexion a la base de datos mysql local -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/gestionturnos?serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- aqui digo a hibernate que dialecto usar para mysql -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>

            <!-- aqui le digo a hibernate que cree/actualice las tablas automaticamente -->
            <property name="hibernate.hbm2ddl.auto" value="update"/> <!--create(en desarrollo) o update-->

            <!-- aqui muestro por consola las consultas SQL que se ejecutan -->
            <property name="hibernate.show_sql" value="true" />
            <property name="hibernate.format_sql" value="true" />
        </properties>
    </persistence-unit>
</persistence>